const gamePrompt = require('./userInput')
//make right input, left input, down input,
//first check if player is next to hat(if?)
//Then ask for an input, input will cause box next character to turn to character(unless it is already a field character"you already went there pick another direction")
